#include <stdlib.h>
#include <iostream>

#include "RingBuffer.h"
#include "Logger.h"

using namespace std;

int main(void)
{
    //Logger::LoggerSetting(LOGLEVEL::DEBUG);
    //Logger log("../", "test");

    exit(0);
}